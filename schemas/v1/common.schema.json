{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://checkspan.invalid/schemas/v1/common.schema.json",
  "title": "Checkspan shared primitives",
  "description": "Identity primitives reused by every Checkspan record. Schema IDs are identifiers under a reserved, never-resolving host; they are served only by the bundled offline registry.",
  "$defs": {
    "identifier": {
      "description": "Stable machine identifier. Lowercase ASCII letters, digits, '_', '.', '-'; must start with a letter or digit; at most 128 bytes. Display names are separate fields and never identity.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9_.-]{0,127}$"
    },
    "revision": {
      "description": "Monotonic contract revision, starting at 1.",
      "type": "integer",
      "minimum": 1,
      "maximum": 4294967295
    },
    "version": {
      "description": "Version of a schema, policy, or verifier, starting at 1.",
      "type": "integer",
      "minimum": 1,
      "maximum": 4294967295
    },
    "schema_version": {
      "description": "Major version of the record schema. Unknown versions are rejected before any other check.",
      "type": "integer",
      "minimum": 1,
      "maximum": 4294967295
    },
    "timestamp": {
      "description": "RFC 3339 date-time with an explicit UTC offset or 'Z'.",
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]{1,9})?(Z|[+-][0-9]{2}:[0-9]{2})$"
    },
    "display_name": {
      "description": "Human-facing label. Carries no identity; two records with the same identifiers are the same record regardless of display name.",
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "digest": {
      "description": "Content digest: 'sha256:' followed by 64 lowercase hex digits. Identifies bytes; does not establish who produced them.",
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "policy_ref": {
      "description": "Reference to a versioned policy.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "version"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "version": { "$ref": "#/$defs/version" }
      }
    },
    "type_ref": {
      "description": "Pinned result schema: name, version, and digest of the schema text.",
      "type": "object",
      "additionalProperties": false,
      "required": ["schema_id", "version", "digest"],
      "properties": {
        "schema_id": { "$ref": "#/$defs/identifier" },
        "version": { "$ref": "#/$defs/version" },
        "digest": { "$ref": "#/$defs/digest" }
      }
    },
    "verifier_ref": {
      "description": "Pinned verifier: name, version, and digest of its code or profile.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "version", "digest"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "version": { "$ref": "#/$defs/version" },
        "digest": { "$ref": "#/$defs/digest" }
      }
    },
    "attempt_number": {
      "description": "Attempt number of one node within one run, starting at 1. Every started attempt counts.",
      "type": "integer",
      "minimum": 1,
      "maximum": 4294967295
    },
    "attempt_ref": {
      "description": "Exact reference to one attempt.",
      "type": "object",
      "additionalProperties": false,
      "required": ["run_id", "node", "number"],
      "properties": {
        "run_id": { "$ref": "#/$defs/identifier" },
        "node": { "$ref": "node-ref.schema.json" },
        "number": { "$ref": "#/$defs/attempt_number" }
      }
    },
    "receipt_ref": {
      "description": "Reference to a verifier receipt: its id, the run it was issued in, and the node it is about.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "run_id", "node"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "run_id": { "$ref": "#/$defs/identifier" },
        "node": { "$ref": "node-ref.schema.json" }
      }
    },
    "provenance_level": {
      "description": "How strongly a record's issuer is authenticated.",
      "enum": ["local_controller", "authenticated_upstream", "signed_operator"]
    },
    "text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048
    }
  }
}
